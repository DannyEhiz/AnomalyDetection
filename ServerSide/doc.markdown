Data Collection:
data is refreshed every 7 days, and existing data is deleted before new ones are saved to the db

Modelling:
Collects the data from db, them uses MinMaxScaler for normalizing the data, then saves the normalizer for subsequent use.